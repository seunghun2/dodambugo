# 2026-09-20 작업 완료 요약 (마음부고 & 대대손손)

## 📌 주요 완료 내역

### 1. 대대손손(daedaesonson.com) 장지 리뷰 자동 동기화 시스템 구축
- **배경**: 마음부고에서 수집된 장지 이용 후기(`burial_reviews`)를 전국 장지 비교 플랫폼인 **대대손손**의 시설 리뷰(`Review`)로 자동 매칭 및 동기화
- **실제 데이터 전수 검증 (22건)**:
  - ✅ **자동 매칭 완료**: 15건 (68%) — 용인공원, 대전추모공원, 서울시립승화원, 울산하늘공원, 익산하늘공원, 이천시립자연장지, 팔당자하연, 김해추모의공원, 안성추모공원, 에덴추모공원, 안동추모공원 등
  - ⏭ **안전 스킵**: 2건 — '화장 후 봉안'(블랙리스트), '부산 기장군'(지역명만 입력)
  - ❌ **미매칭(안전 거부)**: 5건 — 국립현충원, 이천호국원, 느보산추모공원 등 대대손손 공공DB(e하늘)에 미등록된 국가보훈/사설 시설 (오매칭 방지)
  - **오매칭 발생 건수**: **0건 (100% 안전)**
- **동기화 파이프라인**:
  - `jojo/lib/daedaesonson-sync.ts` 신규 생성
  - 1단계: 별칭 사전(ALIAS_MAP: 벽제승화원→서울시립승화원 등) 100% 우선 매칭
  - 2단계: 블랙리스트(선영, 선산, 화장 등) 및 지역명 단독 입력 자동 배제
  - 3단계: 일정/시간 정규식 제거(예: "4월 5일(일) 14:20 영락공원에서 화장" → "영락공원")
  - 4단계: Bigram(Dice's Coefficient) 유사도 75% 이상만 자동 승인
  - 5단계: 대대손손 `Review` 테이블에 `source: 'maeumbugo'`로 INSERT + 해당 시설 `reviewCount`, `rating`(평균 별점) 자동 재계산 UPDATE
- **사용자 경험 보호**:
  - `jojo/app/api/burial-review/route.ts`에서 **비동기 논블로킹(fire-and-forget)** 호출
  - 대대손손 네트워크 지연/에러 발생 시에도 마음부고 사용자는 지연 없이 즉시 200 OK 응답 수신

### 2. 에이전트/도구 탐색 무한로딩 원천 차단 (`.ignore` 도입)
- **원인 분석**: 루트 디렉토리의 2.65GB XD(`상품 모음.xd`), 426MB XD, 226MB PSD 파일 등으로 인해 ripgrep/fd 탐색 시 macOS iCloud Drive I/O 병목 및 프로세스 행(Hang) 발생
- **조치**: 루트에 `.ignore` 파일 배치하여 `*.xd`, `*.psd`, `*.ai`, `*.mov`, `*.zip` 등 거대 바이너리 검색을 즉시 스킵 처리

### 3. 부의금 미지급 건 재송금 크론 라우트 포함
- `jojo/app/api/cron/retry-condolence-transfer/route.ts` 자정 점검 등으로 실패한 입금이체 재처리 크론 추가

---

## 🔑 환경변수 세팅 (Vercel 필수)
마음부고 Vercel 프로젝트 환경변수에 아래 2개 키가 등록되어야 실서버에서 동기화가 동작합니다:
- `DAEDAE_SUPABASE_URL`: `https://jbydmhfuqnpukfutvrgs.supabase.co`
- `DAEDAE_SUPABASE_SERVICE_KEY`: `sb_secret_***` (로컬 .env.local 또는 Vercel 환경변수 참조)
*(로컬 `jojo/.env.local`에는 이미 추가 완료)*
