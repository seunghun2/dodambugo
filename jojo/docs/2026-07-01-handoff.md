# 🗓️ 2026-07-01 작업 기록 — 부고온 앱 출시 진행

> **이 파일은 다른 컴퓨터에서 작업 이어받기 위한 핸드오프 문서입니다.**

---

## ✅ 오늘 완료된 작업

### 1. B2C 마음부고 임시저장 + 화환 결제 버그 수정 (긴급)

**근본 원인**: Supabase PostgREST **스키마 캐시**가 최근 추가된 컬럼을 인식 못 함

| 수정 파일 | 문제 컬럼 | 수정 내용 |
|-----------|----------|----------|
| `jojo/app/api/drafts/route.ts` | `b2b_user_id` | 값이 있을 때만 조건부 포함 |
| `jojo/app/api/flower-orders/route.ts` | `partner_data` | 값이 있을 때만 조건부 포함 |

- **6/21 ~ 6/28 약 일주일간** B2C 화환 주문이 전부 실패하고 있었음
- 임시저장도 동일하게 실패
- Vercel 프로덕션 배포 완료 → API 정상 동작 확인 ✅

---

### 2. Apple Developer 등록 완료

| 항목 | 값 |
|------|-----|
| DUNS 번호 | `696633660` |
| Team ID | `FTR72WNB4B` |
| 등록 유형 | Individual (개인사업자) |
| 결제 | $99 완료 (6/25) |
| 승인 | 완료 (6/29) |
| App Store Connect | 접근 가능 ✅ |

**App Store Connect 앱 등록 완료:**
- 앱 이름: `부고온` (처음에 "부고온2"로 잘못 입력 → 수정 필요!)
- Bundle ID: `kr.co.maeumbugo.bugoon`
- SKU: `bugoon-partner`
- App ID 등록 시 Push Notifications 활성화 ✅

**⚠️ 이름 수정 필요**: App Store Connect → 일반 정보 → 앱 정보에서 "부고온2" → "부고온"으로 변경

---

### 3. Android 빌드 환경 세팅 + AAB 빌드 성공

**설치된 환경 (이 Mac에만 해당):**
- Java JDK 21.0.11 → `/Users/el/.local/java/jdk-21.0.11.jdk/`
- Android SDK → `/Users/el/.local/android/`
- 환경변수 → `~/.zshrc`에 추가됨

**변경된 파일:**
- `jojo/capacitor.config.ts` — appId를 `kr.co.maeumbugo.bugoon`으로 변경
- `jojo/android/app/build.gradle` — namespace/applicationId 변경 + 서명 설정 추가
- `jojo/android/app/google-services.json` — 패키지명 변경
- `jojo/android/local.properties` — Android SDK 경로 (로컬 전용, git 무시됨)

**서명 키(Keystore) 정보:**
- 위치: `jojo/android/bugoon-release.keystore`
- Alias: `bugoon`
- 비밀번호: `bugoon2026`
- ⚠️ **이 키를 잃어버리면 앱 업데이트 불가! 반드시 백업할 것**

**빌드 산출물:**
```
jojo/android/app/build/outputs/bundle/release/app-release.aab (4.6MB)
```

**다른 컴퓨터에서 Android 재빌드하려면:**
```bash
# Java 21 + Android SDK 설치 필요
export JAVA_HOME=/path/to/jdk-21
export ANDROID_HOME=/path/to/android-sdk

cd /Users/el/Desktop/dodam/jojo/android
./gradlew bundleRelease
```

---

### 4. Google Play Console 등록 중 (미완료)

**현재 막힌 곳**: 조직 웹사이트 소유권 인증
- URL: `https://bugoon.maeumbugo.co.kr/`
- Google Search Console에서 도메인 소유권 확인 필요
- 방법: Search Console → 속성 추가 → DNS 레코드 또는 HTML 태그 인증

**Google Play Console 계정:**
- Google 계정: `wsh9991@gmail.com`
- 계정 유형: 기관/단체 (회사 또는 비즈니스)
- $25 결제: 아직 안 한 것으로 보임 (인증 단계에서 막힘)

---

## ⏳ 남은 작업 (우선순위 순)

### 즉시 필요
1. **Google Search Console에서 `bugoon.maeumbugo.co.kr` 소유권 인증**
2. **Google Play Console 등록 완료** ($25 결제)
3. **App Store Connect에서 앱 이름 "부고온2" → "부고온" 수정**

### iOS 앱 제출 (Xcode 설치 필요)
4. Mac App Store에서 Xcode 설치 (약 12GB)
5. Xcode Signing 설정 (Team 연결)
6. `npx cap sync ios`
7. Xcode Archive → App Store 업로드
8. 앱 심사 제출

### Android 앱 제출
9. Google Play Console에서 앱 만들기
10. AAB 파일 업로드 (`app-release.aab`)
11. 스토어 등록 정보 작성 (스크린샷, 설명, 개인정보처리방침)
12. 앱 심사 제출

### Firebase 정식 등록 (푸시 알림용)
13. Firebase Console → 프로젝트 `bugoapp-23680` → 앱 추가
14. Android 패키지명 `kr.co.maeumbugo.bugoon`으로 정식 등록
15. 새 `google-services.json` 다운로드 → `jojo/android/app/` 에 교체

### 공통 준비물
16. 앱 아이콘 (1024x1024)
17. 스크린샷 (iPhone 6.5", Android 폰/태블릿)
18. 개인정보처리방침 URL
19. 앱 설명문 (한국어)

---

## 📁 관련 문서 경로
- 앱 제출 전체 체크리스트: `jojo/docs/APP_SUBMISSION.md`
- DUNS 정보: `jojo/docs/DUNS_INFO.md`
- B2B 앱 태스크 스킬: `jojo/.agents/skills/b2b-app-task/SKILL.md`
- 배포 스킬: `jojo/.agents/skills/deploy/SKILL.md`
