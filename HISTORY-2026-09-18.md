# 2026-09-18 작업 완료 요약 (마음부고 & 부고온)

## 📌 주요 완료 내역

### 1. B2B(부고온) 제작기 호칭 옵션 반전
- 기존: `[ ] 부고장 호칭 노출` (체크 안 하면 호칭 미노출)
- 변경: **`[ ] 부고장 호칭 미노출`** (기본 노출, 체크 시 미노출)
- 파일: `jojo/app/b2b/create/sections/DeceasedSection.tsx`, `jojo/app/b2b/create/page.tsx`

### 2. B2B(부고온) 뷰어 종교 심볼 뱃지 시스템 이식 및 레이아웃 튜닝
- 기독교(빨강), 천주교(서울대교구 자주색 `#7b0039`), 불교(만자 빨강 `#de0615`), 원불교(골드 `#eaad2d`) 컬러 심볼 적용
- 모서리 8px 라운드, 투명 배경, 회색 테두리(`1px solid #d4d7de`), 텍스트 하향 2px 보정
- **영정사진 있을 때**: 종교 뱃지 위치 20px 하향 조정
- **영정사진 없을 때**: 고인명과 설명문 간격 20px 축소
- 파일: `jojo/app/b2b/view/[id]/ViewContent.tsx`, `jojo/app/b2b/view/[id]/view.css`

### 3. B2C(마음부고) 종교 뱃지 개선 및 SGI 제거
- 천주교 자주색 및 불교 만자 SVG 교체
- SGI 옵션 전면 제거
- 파일: `jojo/app/view/[id]/ViewContent.tsx`, `jojo/app/view/[id]/view.css`, `jojo/app/create/[templateId]/page.tsx`

### 4. 화환 결제 안전성 확보 (핵심 비즈니스 보호)
- 로컬 개발 시 임시 적용했던 B2C `cachedProducts` (10분 인메모리 캐시) 완전 제거
- 100% 안전한 실시간 DB 조회로 원복하여 빈 상품 캐싱 위험 차단
- Jest 단위 테스트 83개 전원 통과 검증
- 파일: `jojo/app/view/[id]/page.tsx`

### 5. INNOPAY 결제창 상점명 질의 분석
- 결제창 상점명 '마음부고'는 가맹점 ID(MID)에 귀속된 PG사 고정 상점명임
- B2B/B2C 분리 표기를 위해서는 이노페이 서브 MID 추가 발급 후 코드 분기 필요
